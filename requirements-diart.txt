# Optional isolated CPU runtime for the Diart real-time diarization pilot.
# Install only in Dockerfile.diart (Python 3.10/Linux). Do not combine this
# graph with the Python 3.12 core, GigaAM, live capture, or sherpa runtimes.
--extra-index-url https://download.pytorch.org/whl/cpu

diart==0.9.2
huggingface-hub==0.23.5
lightning==2.2.5
numpy==1.26.4
onnxruntime==1.27.0
pyannote.audio==3.1.1
scipy==1.12.0
soundfile==0.12.1
torch==2.2.2+cpu
torchaudio==2.2.2+cpu
torchmetrics==1.3.2
torchvision==0.17.2+cpu
