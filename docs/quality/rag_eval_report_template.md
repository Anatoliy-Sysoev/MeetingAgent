# Шаблон Отчета По Оценке RAG

Обновлено: 2026-05-07.

Этот файл используется для исторических срезов качества `FTT-MA-07`.

## Метаданные Прогона

- Дата прогона:
- Исполнитель:
- Git commit: полный SHA коммита
- Git status перед прогоном: чистый / есть изменения
- Embedding-модель:
- Чат-модель:
- Backend поиска: numpy / faiss / hybrid / другое
- `top_k`:
- `chunk_size_chars`:
- `chunk_overlap_chars`:
- Количество chunks:
- `chunks.jsonl` SHA256:
- `chunks.jsonl` mtime:
- `chunks.jsonl` size:
- `embeddings_cache.jsonl` size:
- `embeddings_cache.jsonl` lines:
- Корпус / источник:
- Raw-лог прогона: путь к локальному `logs/*.jsonl`, не коммитится
- Точная команда прогона:

## Критерий Приемки

- Порог приемки:
- Статус порога: зафиксирован / обсуждается / не применялся

## Правило Интерпретации

- `hit`: ожидаемый источник или тип источника найден в top-k и может быть использован для ответа.
- `partial`: найден близкий или частичный источник, но выдача неполная, шумная или пропускает ключевой ожидаемый документ.
- `miss`: ожидаемые источники фактически не найдены или выдача уводит в другой тип документов.

## Итог По Прогону

- Всего вопросов:
- `hit`:
- `partial`:
- `miss`:
- Вопросов с дубликатами в top-k:
- Вопросов с системными экспортами в top-3:
- Ключевые дубли:
- Главные проблемы:
- Решение по следующему шагу:

## Результаты По Вопросам

### Вопрос N

- Категория:
- Тип запроса: factoid / aggregation / definitional / procedural
- Вопрос:
- Ожидаемые источники:
- Ожидаемое количество релевантных источников: опционально
- Команда:
- Фактический top-k:
- Вердикт: `hit | partial | miss`
- Комментарий:
- Дубли:
- Системные экспорты в top-k:
- Что проверить позже:

## Наблюдения

- Что ищется хорошо:
- Что ищется плохо:
- Какие типы документов доминируют в выдаче:
- Есть ли шум от backup-версий:
- Нужен ли reranking:
- Нужна ли чистка корпуса:

## Решение После Прогона

- Порог приемки, если зафиксирован:
- Переходить ли к следующему этапу:
- Что улучшать до следующего прогона:
- Какие изменения запрещено смешивать с baseline:
