# Что Делаем, Пока Собирается RAG

Обновлено: 2026-05-06.

## Зачем Этот Документ

Текущая сборка RAG занимает много времени. Ждать пассивно не нужно: значительную часть продукта можно проектировать и реализовывать параллельно, не трогая живой `03_build_index.py` и не мешая Ollama.

Ниже задачи разбиты на этапы так, чтобы было понятно:

- что можно делать прямо сейчас;
- что требует готового RAG;
- что требует тестового видео;
- что лучше отложить до появления API/UI.

## Правила Параллельной Работы

- Не убивать живой `03_build_index.py`.
- Не удалять `data/embeddings_cache.jsonl`.
- Не трогать `vector_db/` до завершения текущей сборки.
- Не запускать второй полный build.
- Все продуктовые изменения фиксировать в Git.
- Runtime-данные, видео, транскрипты реального проекта и `config.yaml` не коммитить.

## Этап A. Продуктовая Упаковка

Статус: можно делать сейчас.

Цель: чтобы проект был понятен как продукт, а не только как набор скриптов.

Задачи:

- Уточнить продуктовые термины: проект, источник, chunk, встреча, решение, задача, brief.
- Поддерживать `docs/product/PRODUCT_VISION_AND_PLAN.md` как главный документ видения.
- Сделать короткий `docs/product/GLOSSARY.md` с терминами: ФТТ, Паспорт ИС, memo, протокол, source citation, meeting card.
- Создать `docs/product/USER_SCENARIOS.md` с пошаговыми сценариями:
  - спросить проект;
  - обработать встречу;
  - сгенерировать Паспорт ИС;
  - обновить RAG;
  - разобрать спорную классификацию.
- Описать роли пользователя: РП, аналитик, архитектор, ответственный за сдачу.

Готовность:

- новый тред может прочитать документацию и понять, что строится;
- можно объяснить продукт другому человеку за 5 минут.

## Этап B. Шаблоны И Prompt-Пакет

Статус: можно делать сейчас.

Цель: заранее подготовить инструкции, по которым агент будет писать memo, протоколы, задачи и документы.

Задачи:

- Создать папку `templates/prompts/meetings/`.
- Создать prompt-шаблоны:
  - `meeting_summary.md`;
  - `meeting_protocol.md`;
  - `meeting_decisions.md`;
  - `meeting_tasks.md`;
  - `meeting_risks.md`;
  - `meeting_classification.md`.
- Создать папку `templates/prompts/documents/`.
- Создать prompt-шаблоны:
  - `passport_is.md`;
  - `ftt_extract.md`;
  - `architecture_note.md`;
  - `project_solution.md`;
  - `handover_checklist.md`.
- Добавить правила источников:
  - не придумывать факты без sources;
  - помечать пробелы;
  - отделять выводы от подтвержденных данных.

Готовность:

- каждый тип результата имеет отдельный prompt;
- prompt можно использовать как из CLI, так и из будущего API.

## Этап C. Форматы Артефактов

Статус: можно делать сейчас.

Цель: заранее определить, как будут выглядеть файлы, которые продукт создает.

Задачи:

- Детализировать `docs/templates/MEETING_CARD.md`.
- Создать JSON/YAML-схемы в `configs/schemas/`:
  - `meeting_card.schema.json`;
  - `meeting_artifacts.schema.json`;
  - `classification.schema.json`;
  - `document_brief.schema.json`;
  - `task.schema.json`;
  - `decision.schema.json`.
- Описать структуру папки встречи:

```text
meetings/
  2026-05-06__meeting-title/
    source/
    transcript.md
    memo.md
    protocol.md
    decisions.json
    tasks.json
    risks.json
    classification.json
    routing.json
```

- Описать правила именования файлов.

Готовность:

- понятно, куда сохранять результаты обработки встречи;
- будущие скрипты не будут изобретать формат заново.

## Этап D. CLI-Каркас

Статус: можно делать сейчас.

Цель: подготовить единый способ запускать функции продукта.

Задачи:

- Создать модуль `src/meeting_agent/cli/`.
- Спроектировать команды:

```powershell
meeting-agent status
meeting-agent rag build
meeting-agent rag update
meeting-agent query "..."
meeting-agent meeting process "path-to-video"
meeting-agent document generate "brief.md"
```

- Пока можно сделать thin wrapper вокруг существующих скриптов.
- Добавить `pyproject.toml` позже, когда начнем оформлять пакет.

Готовность:

- команды продукта описаны;
- будущий CLI не конфликтует с текущими PowerShell-скриптами.

## Этап E. Статус И Диагностика

Статус: можно делать сейчас.

Цель: сделать так, чтобы всегда было понятно, что происходит.

Задачи:

- Улучшить `check_rag_status.ps1`:
  - показывать process PID;
  - cache count;
  - total chunks;
  - скорость прироста cache;
  - последний log tail;
  - текущий этап build.
- Добавить `scripts/00_healthcheck.py`:
  - проверка Python packages;
  - проверка Ollama `/api/tags`;
  - проверка embedding `bge-m3` с `num_ctx=8192`;
  - проверка наличия config;
  - проверка путей.
- Добавить `docs/operations/TROUBLESHOOTING.md`.

Готовность:

- пользователь может одной командой понять, жив ли процесс;
- типовые ошибки описаны.

## Этап F. Query UX И Источники

Статус: частично можно сейчас, полноценно после готового RAG.

Цель: подготовить формат ответа, чтобы потом быстро улучшить `04_query.py`.

Можно сейчас:

- Описать формат ответа:
  - краткий ответ;
  - подробности;
  - источники;
  - пробелы/неуверенность;
  - связанные документы.
- Создать `docs/templates/QUERY_RESPONSE.md`.
- Создать `docs/quality/rag_eval_questions.md` с 10-20 вопросами.
- Подготовить критерии оценки источников.

После готового RAG:

- прогнать evaluation-вопросы;
- улучшить ranking/formatting;
- добавить citations в `scripts/04_query.py`.

Готовность:

- есть список вопросов;
- есть единый формат ответа;
- после завершения RAG можно сразу тестировать.

## Этап G. Meeting Processor Без RAG

Статус: можно делать сейчас, если есть тестовое видео или аудио.

Цель: научиться обрабатывать одну встречу до подключения классификации через RAG.

Задачи:

- Создать `scripts/05_process_meeting.py`.
- Вход: путь к аудио/видео.
- Выход:
  - `transcript.md`;
  - `memo.md`;
  - `protocol.md`;
  - `tasks.json`;
  - `decisions.json`;
  - `risks.json`.
- Использовать `faster-whisper`.
- Сначала без speaker diarization.
- Добавить safe output folder в `watched_folder/_processed/` или отдельную runtime-папку.

Готовность:

- один файл вручную превращается в комплект артефактов;
- результаты пока не добавляются в RAG автоматически.

## Этап H. Классификация Без Готового RAG

Статус: можно проектировать сейчас, реализация после RAG.

Цель: определить, как MeetingAgent будет понимать этапы, ФТТ, документы и задачи.

Можно сейчас:

- Создать `configs/schemas/classification.schema.json`.
- Описать scoring:
  - `project_stage`;
  - `ftt_refs`;
  - `task_refs`;
  - `document_refs`;
  - `confidence`;
  - `reasons`;
  - `alternatives`.
- Описать пороги:
  - `>=0.75` - можно маршрутизировать;
  - `0.45-0.75` - предложить пользователю;
  - `<0.45` - в ручной разбор.

После готового RAG:

- искать похожие chunks;
- классифицировать реальные встречи;
- сохранять `routing.json`.

Готовность:

- формат классификации готов до появления алгоритма.

## Этап I. Генерация Документов

Статус: можно проектировать сейчас, реализация после RAG.

Цель: подготовить генерацию документов так, чтобы она не была хаотичной.

Можно сейчас:

- Детализировать `docs/templates/DOCUMENT_GENERATION_BRIEF.md`.
- Создать briefs:
  - `templates/documents/passport_is.brief.md`;
  - `templates/documents/ftt_extract.brief.md`;
  - `templates/documents/architecture_note.brief.md`;
  - `templates/documents/project_solution.brief.md`.
- Описать checklist проверки:
  - есть ли источники;
  - есть ли пробелы;
  - нет ли неподтвержденных утверждений;
  - соответствует ли структура документу.

После готового RAG:

- реализовать generator;
- протестировать на Паспорт ИС.

Готовность:

- понятно, как пользователь будет задавать документ;
- генератор можно писать без споров о формате.

## Этап J. Local API

Статус: можно проектировать сейчас, писать позже.

Цель: подготовить продукт к UI и автоматизациям.

Можно сейчас:

- Создать `docs/architecture/API_DESIGN.md`.
- Описать endpoints:
  - `GET /status`;
  - `POST /rag/build`;
  - `POST /rag/update`;
  - `POST /query`;
  - `POST /meetings/process`;
  - `GET /meetings/{id}`;
  - `POST /documents/generate`;
  - `GET /jobs/{id}`.
- Описать job model.
- Описать error model.

Позже:

- реализовать FastAPI;
- добавить background jobs;
- подключить UI.

Готовность:

- API понятен до реализации;
- UI можно проектировать по контрактам.

## Этап K. UI-Концепция

Статус: можно проектировать сейчас.

Цель: понять, какой интерфейс нужен, прежде чем писать frontend.

Экраны:

- Dashboard/status;
- inbox встреч;
- карточка встречи;
- поиск по проекту;
- генерация документа;
- ручной разбор классификации;
- настройки.

Что можно сделать сейчас:

- `docs/product/UI_CONCEPT.md`;
- wireframe-описания экранов;
- список кнопок и действий;
- состояния: queued, processing, failed, completed, needs_review.

Готовность:

- UI не начинается с пустого листа;
- понятно, что должен делать первый экран.

## Этап L. Speakr-Адаптация

Статус: можно делать сейчас.

Цель: превратить идеи Speakr в наши конкретные требования.

Задачи:

- Расписать `docs/references/SPEAKR_ADAPTATION.md`.
- Таблица:
  - функция Speakr;
  - зачем нам;
  - как адаптируем;
  - приоритет;
  - входит ли в MVP.
- Отдельно описать:
  - prompt-теги;
  - карточка записи;
  - audio-transcript sync;
  - Inquire Mode;
  - REST API;
  - retention/protected records;
  - speaker identification.

Готовность:

- идеи Speakr превращены в задачи MeetingAgent;
- нет риска случайно копировать код.

## Приоритет На Ближайшие 1-2 Сессии

Пока RAG строится, самые полезные задачи:

1. Сделать prompt-шаблоны встреч.
2. Сделать схемы JSON для meeting card, tasks, decisions, classification.
3. Сделать `docs/quality/rag_eval_questions.md`.
4. Улучшить `check_rag_status.ps1`.
5. Спроектировать `05_process_meeting.py`.
6. Сделать `docs/references/SPEAKR_ADAPTATION.md`.

## Что Не Делаем Пока

- Не пишем полноценный UI.
- Не трогаем текущую vector DB.
- Не запускаем второй full RAG build.
- Не внедряем cloud API.
- Не делаем multi-user/SSO.
- Не делаем speaker voice profiles до стабильной базовой транскрибации.

## Рекомендуемый Следующий Шаг

Самый продуктивный следующий шаг:

> Создать prompt-пакет и схемы артефактов встречи.

Почему:

- не зависит от готового RAG;
- сразу пригодится для `05_process_meeting.py`;
- задает формат продукта;
- хорошо переносится в будущий API/UI.

