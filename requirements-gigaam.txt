# Optional runtime dependencies for local GigaAM backend.
# Install in a Python version with prebuilt onnx wheels available.
# On Windows/Python 3.14, onnx==1.19.* may attempt a source build and fail on path length.
hydra-core==1.3.*
omegaconf==2.3.*
onnx==1.19.*
onnxruntime==1.25.*
soundfile
sentencepiece
torchaudio>=2.6
